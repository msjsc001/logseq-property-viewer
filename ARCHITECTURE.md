# Property Query 架构说明

## 目标
本项目的目标是：在本地对 Logseq / Markdown 图谱进行可信、高性能、可增量更新的属性查询与统计，并通过桌面 GUI 暴露稳定可用的用户体验。

## 核心设计

### 查询语义
- `has:key`：属性存在
- `key:value`：精确匹配，默认大小写不敏感，按完整值比较
- `key~value`：模糊匹配，默认大小写不敏感，按子串比较
- `AND / OR`：逻辑组合，`AND` 优先级高于 `OR`
- 裸词：全文搜索，匹配页面名、块内容、属性名、属性值
- 查询页支持“大小写敏感”开关；开启后 `has / exact / fuzzy / 全文` 全部切到原始大小写比较

Why：
- 旧实现把 `key:value` 当成子串匹配，导致 `status:done` 会误命中 `undone`
- 文档和实现必须严格一致，否则查询结果不可信

### Block 数据模型
缓存和搜索结果统一使用以下字段：
- `page`
- `file_path`
- `block_content`
- `line_start`
- `line_end`
- `block_path`
- `properties`

Why：
- 旧实现按顶层 `- ` 粗暴切块，嵌套子块属性会串到父块
- 新模型保留块上下文与行号，便于统计、导出和块级定位

### 属性解析策略
- 解析范围包含两类：
  - 页首属性区：文件开头、首个真正 block 之前的连续 `key:: value` 行
  - block 属性区：某个 block 下缩进更深的属性行，或 block label 自身就是 `key:: value`
- 属性值按原始字符串保留，不强拆 wiki-link、多值空格分隔、半角/全角逗号分隔
- 键名过滤规则偏保守：
  - 允许中文、英文、数字、连字符、下划线等真实键
  - 拒绝空键
  - 拒绝只由分隔符/数字组成、没有任何字母或中文的伪键，如 `--`、`--2`

Why：
- Logseq 的页面属性经常出现在第一个 `- block` 之前，若不单独支持就会整体漏检
- 过于宽松地把所有 `::` 行都当属性，会把视觉分隔符和正文噪音污染进索引

### 扫描与监听策略
- 标准图谱：优先扫描 / 监听 `pages/` 与 `journals/`
- 非标准目录：递归扫描 / 监听整个根目录下的 Markdown 文件
- 排除 `node_modules`、`dist`、`build`、隐藏目录等非图谱内容

Why：
- 扫描策略与 watcher 必须完全一致，否则会出现“能扫到但监听不到”的失配

### 缓存策略
- 缓存文件存放在 `%APPDATA%\PropertyQuery\cache`
- 缓存结构带 `schema_version`
- 属性解析语义变化也会提升 `schema_version`，确保老缓存不会继续漏掉页首属性
- 旧 schema、损坏缓存、缺失缓存都会在首次查询 / 统计时自动重建

Why：
- 旧缓存结构无法表达新 block 模型，必须版本化
- 自动判定失效并重建，避免升级后让用户手动排障

### 启动与握手
- `run_app.py` 不再固定使用 `8000`，而是动态寻找本地空闲端口
- 启动成功判定不是“端口通了”，而是 `/api/health` 返回本应用签名 `property-query`
- 启动日志落地到 `%APPDATA%\PropertyQuery\logs`

Why：
- 旧实现只看端口可连，任何占用 `8000` 的本地服务都会被误判为后端

### 状态持久化
后端统一持久化：
- 图谱路径
- 语言
- 查询历史
- 全局隐藏列
- 列配置
- 侧边栏状态
- 自动更新开关

前端 `localStorage` 仅作为短期缓存，不再是唯一真源。

## 测试防线

### 后端
- 查询语义单测
- 嵌套块归属单测
- 非标准目录递归扫描单测
- 旧缓存 schema 失效判定单测
- watcher 路径切换单测
- 启动握手单测

### 前端
- i18n 首屏从后端偏好恢复
- CSV 导出带 UTF-8 BOM，确保 Excel 中文兼容
- `lint / test / build` 作为最低交付门槛

## 目录约定
- 数据目录：`%APPDATA%\PropertyQuery`
- 日志目录：`%APPDATA%\PropertyQuery\logs`
- 缓存目录：`%APPDATA%\PropertyQuery\cache`

## 当前取舍
- Logseq block 精确 URI 在不同版本间稳定性不足，因此“定位块”默认采用优雅回退：先打开页面，再提示行号范围
- 统计页现在同时支持“键统计”和“值统计”：键统计用于观察属性名分布，值统计用于观察全局值分布并下钻查看它出现在哪些键里
- 全局值统计默认按原始值精确聚合，保留大小写差异；空字符串仍参与统计，但界面显示为“(空值)”
- 前端已按页面懒加载和 vendor 拆包，但 Ant Design 与表格相关依赖仍是主要体积来源，后续若继续压缩体积，可再细化按需拆分
